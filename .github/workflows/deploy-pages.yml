# Build with Vite and publish to GitHub Pages.
#
# One-time repo setup (after merging this file):
#   1. Settings → Pages → Build and deployment → Source: GitHub Actions
#   2. Settings → Actions → General → Workflow permissions:
#      enable "Read and write permissions" (needed for Pages deployment)
#
# Custom domain: https://tokemonlabs.com
# The workflow sets VITE_BASE_PATH=/ because the site is served at the
# domain root. `public/CNAME` tells GitHub Pages which hostname to use.

name: Deploy to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Install
        run: npm ci

      - name: Build
        run: npm run build
        env:
          # Custom-domain Pages is served at the domain root.
          VITE_BASE_PATH: "/"

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
